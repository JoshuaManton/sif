#include "core:basic.sif"
// #include "core:dynamic_array.sif"

proc main() : i32 {
    var a = 123;
    var b: >int = a;
    b = 321;
    assert(a == 321);

    var d = 123;
    var e = cast(float, d);
}
