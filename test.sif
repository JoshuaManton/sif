#include "core:basic.sif"

proc stuff(a: any) {
    var i = cast(^[2]int, a.data);
    i^[1] = 321;
}

struct Foo {
    var a: int;
}

proc main() : i32 {
    var i = 12;
    var a = 1 >> (i+2);

    var f: Foo;
    var arr: [2]int;
    stuff(arr);
    print("%\n", arr[1]);
    print("this is % very % neato 100%%\n", 123.4, true);
    printa(123, 42.1, true, "hellooooooo");
    printa(123, 42.1, true, "hellooooooo");
}
