#include "core:basic.sif"

struct Foo {
    using var bar: Bar;
}

struct Bar {
    using var baz: Baz;
}

struct Baz {
    var qux: int;
}

proc main() : i32 {
    using Baz;
    print("%\n", AAA);
    var arr: [4][4]Foo;
    arr[2][2].qux = 321;
    using arr[2][2];
    print("%\n", qux);
    return 0;
}