#include "core:basic.sif"

struct Foo {
    var bar: Bar;
}

struct Bar {
    var baz: Baz;
}

struct Baz {
    var qux: int;
}

enum My_Enum {
    ENUM_VALUE;
}

proc main() : i32 {
    var foo: Foo;
    foo.bar.baz.qux = 321;
    print("%\n", foo);
    return 0;
}