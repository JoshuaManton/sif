#include "core:basic.sif"

proc stuff(a: any) {
    var i = cast(^[2]int, a.data);
    i^[1] = 321;
}

struct Foo {
    var a: int;
}

proc main() : i32 {
    var f: Foo;
    var arr: [2]int;
    stuff(arr);
    print("%\n", arr[1]);
    print("this is % very % neato 100%%\n", 123.4, true);
}
