proc foo(var x: int) {
    bar(1);
}

proc bar(var x: int) {
    foo(2);
}

#print(sizeof(typeof(a)))
#print(sizeof(T))

var a: [sizeof(typeof(p^))]int;
var p: ^T;
struct T {
    var a: [sizeof(typeof(p))]bool;
}