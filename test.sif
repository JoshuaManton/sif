struct Foo {
    using var bar: ^Bar;
}

struct Bar {
    var baz: int;
}

proc bazzle(x: int, a: string) {

}

proc main() {
    using var foo: Foo;
    bar = new(Bar, default_allocator());
    baz = 123;
    printa(baz);

    var a: int;
    a = 321;
    var b: int;
    b = a;

    bazzle(321, "wowza");
}