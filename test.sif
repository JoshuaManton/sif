#include "core:basic.sif"
#include "core:math.sif"
// #include "core:dynamic_array.sif"

proc procedure(v: [3]float) {
    print("%\n", v);
}

proc main() : i32 {
    procedure({1, 2, 3});
    return 0;
}
