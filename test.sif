#include "core:basic.sif"
// #include "core:dynamic_array.sif"

proc varargs(things: ..int) {
    print_string("--------\n");
    for (var i = 0; i < things.count; i += 1) {
        print_int(things[i]);
    }
}

proc main() : i32 {
    varargs(1, 2, 3);
    varargs(1);
    // todo(josh)
    varargs();
}
