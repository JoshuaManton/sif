#include "core:basic.sif"

proc a(args: ..any) {
    print("---------\n");
    for (var i = 0; i < args.count; i += 1) {
        print("%\n", args[i]);
    }
}

proc b(args: ..any) {
    a(..args);
}

proc main() : i32 {
    b();
    b(1, 2);
    return 0;
}
