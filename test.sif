struct Foo {
    var a: int;
}

proc main() {
    var foo: ^Foo;
    // var foo = new(Foo, default_allocator());
    if (foo != null && foo.a == 0) {
        print("true\n");
    }
    else {
        print("false\n");
    }
}