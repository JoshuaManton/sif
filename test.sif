#include "core:basic.sif"

struct Foo {
    using var bar: Bar;
}

struct Bar {
    using var baz: Baz;
}

struct Baz {
    var qux: int;
}

enum My_Enum {
    ENUM_VALUE;
}

proc main() : i32 {
    var foo: Foo;
    foo.qux = 321;
    print("%\n", foo);
    return 0;
}